pipeline {
    agent any

    tools {
        jdk 'JDK'
        maven 'Maven'
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'master',
                    credentialsId: 'gmail',
                    url: 'https://github.com/NamarataSharma/V3Practice.git'
            }
        }

        stage('Run Provar Tests') {
            steps {
                dir('Demo') {
                    bat 'ant -f ANT/build.xml'
                }
            }
        }
    }

    post {
        always {
            junit '**/ANT/reports/*.xml'
        }
    }
}
